name: Supply Chain (SBOM + Cosign + SLSA)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

permissions:
  contents: read
  packages: write
  id-token: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAMESPACE: ${{ github.repository }}

jobs:
  build-core:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/core-service
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
        if: ${{ !env.ACT }}
        with:
          fetch-depth: 0
      - name: Configure git safe directory
        if: ${{ !env.ACT }}
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: ${{ !env.ACT }}
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/core-service
      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./src/services/core-dotnet/AetherGuard.Core/Dockerfile
          push: ${{ !env.ACT }}
          load: ${{ env.ACT }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Generate SBOM
        if: ${{ !env.ACT }}
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/core-service@${{ steps.build.outputs.digest }}
          registry-username: ${{ github.actor }}
          registry-password: ${{ secrets.GITHUB_TOKEN }}
          format: spdx-json
          output-file: sbom-core.spdx.json
      - name: Upload SBOM
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: sbom-core
          path: sbom-core.spdx.json
      - name: Install cosign
        if: ${{ !env.ACT }}
        uses: sigstore/cosign-installer@v3
      - name: Sign image
        if: ${{ !env.ACT }}
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/core-service@${{ steps.build.outputs.digest }}

  provenance-core:
    needs: build-core
    if: ${{ github.actor != 'nektos/act' }}
    permissions:
      actions: read
      contents: read
      packages: write
      id-token: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0
    with:
      image: ${{ needs.build-core.outputs.image }}
      digest: ${{ needs.build-core.outputs.digest }}

  build-ai:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/ai-service
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
        if: ${{ !env.ACT }}
        with:
          fetch-depth: 0
      - name: Configure git safe directory
        if: ${{ !env.ACT }}
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: ${{ !env.ACT }}
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/ai-service
      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./src/services/ai-engine
          file: ./src/services/ai-engine/Dockerfile
          push: ${{ !env.ACT }}
          load: ${{ env.ACT }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Generate SBOM
        if: ${{ !env.ACT }}
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/ai-service@${{ steps.build.outputs.digest }}
          registry-username: ${{ github.actor }}
          registry-password: ${{ secrets.GITHUB_TOKEN }}
          format: spdx-json
          output-file: sbom-ai.spdx.json
      - name: Upload SBOM
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: sbom-ai
          path: sbom-ai.spdx.json
      - name: Install cosign
        if: ${{ !env.ACT }}
        uses: sigstore/cosign-installer@v3
      - name: Sign image
        if: ${{ !env.ACT }}
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/ai-service@${{ steps.build.outputs.digest }}

  provenance-ai:
    needs: build-ai
    if: ${{ github.actor != 'nektos/act' }}
    permissions:
      actions: read
      contents: read
      packages: write
      id-token: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0
    with:
      image: ${{ needs.build-ai.outputs.image }}
      digest: ${{ needs.build-ai.outputs.digest }}

  build-web:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/web-service
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
        if: ${{ !env.ACT }}
        with:
          fetch-depth: 0
      - name: Configure git safe directory
        if: ${{ !env.ACT }}
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: ${{ !env.ACT }}
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/web-service
      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./src/web/dashboard
          file: ./src/web/dashboard/Dockerfile
          push: ${{ !env.ACT }}
          load: ${{ env.ACT }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Generate SBOM
        if: ${{ !env.ACT }}
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/web-service@${{ steps.build.outputs.digest }}
          registry-username: ${{ github.actor }}
          registry-password: ${{ secrets.GITHUB_TOKEN }}
          format: spdx-json
          output-file: sbom-web.spdx.json
      - name: Upload SBOM
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: sbom-web
          path: sbom-web.spdx.json
      - name: Install cosign
        if: ${{ !env.ACT }}
        uses: sigstore/cosign-installer@v3
      - name: Sign image
        if: ${{ !env.ACT }}
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/web-service@${{ steps.build.outputs.digest }}

  provenance-web:
    needs: build-web
    if: ${{ github.actor != 'nektos/act' }}
    permissions:
      actions: read
      contents: read
      packages: write
      id-token: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0
    with:
      image: ${{ needs.build-web.outputs.image }}
      digest: ${{ needs.build-web.outputs.digest }}

  build-agent:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/agent-service
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
        if: ${{ !env.ACT }}
        with:
          fetch-depth: 0
      - name: Configure git safe directory
        if: ${{ !env.ACT }}
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: ${{ !env.ACT }}
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/agent-service
      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./src/services/agent-cpp/Dockerfile
          push: ${{ !env.ACT }}
          load: ${{ env.ACT }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Generate SBOM
        if: ${{ !env.ACT }}
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/agent-service@${{ steps.build.outputs.digest }}
          registry-username: ${{ github.actor }}
          registry-password: ${{ secrets.GITHUB_TOKEN }}
          format: spdx-json
          output-file: sbom-agent.spdx.json
      - name: Upload SBOM
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: sbom-agent
          path: sbom-agent.spdx.json
      - name: Install cosign
        if: ${{ !env.ACT }}
        uses: sigstore/cosign-installer@v3
      - name: Sign image
        if: ${{ !env.ACT }}
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/agent-service@${{ steps.build.outputs.digest }}

  provenance-agent:
    needs: build-agent
    if: ${{ github.actor != 'nektos/act' }}
    permissions:
      actions: read
      contents: read
      packages: write
      id-token: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0
    with:
      image: ${{ needs.build-agent.outputs.image }}
      digest: ${{ needs.build-agent.outputs.digest }}
